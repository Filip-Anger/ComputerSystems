{Filip Anger 2254974}
{Marek Richnak 2287412}
{WHEN TAKEN LONG BNE disp (when taken) }

IR <- RAM[SigmaA]
{IR = RAM[ip + 1]}

IP, CC <- SigmaA(Bbus) + IR.compl(Abus), ALU.cc
{CC = alu.cc, IP = ip + 1 + RAM[ip + 1]}

SigmaA, IP <- IP, IP + 1
{CC = alu.cc, IP = ip + 2 + RAM[ip + 1], SigmaA = ip + 1 + RAM[ip + 1]}

IR, SigmaA, IP <- RAM[SigmaA], IP, IP + 1
{CC = alu.cc, IP = ip + 3 + RAM[ip + 1], SigmaA = ip + 2 + RAM[ip + 1], IR =  ip + 1 + RAM[ip + 1]}
